name: .NET Core

on:
  push:
    branches: [ dotnet ]
  pull_request:
    branches: [ dotnet ]

jobs:
  build:
    strategy:
      matrix:
        os: ['ubuntu-latest', 'windows-latest', 'macos-latest']
    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout LightningQueues
      uses: actions/checkout@v2
      with:
        path: LightningQueues
    - name: Checkout lmdb
      uses: actions/checkout@v2
      with:
        repository: LMDB/lmdb
        ref: mdb.master
        path: lmdb
      if: matrix.os == 'ubuntu-latest'
    - name: Build lmdb native lib
      run: |
        cd ${{ runer.workspace}}/lmdb/libraries/liblmdb/
        make
        export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:${{ runner.workspace }}/lmdb/libraries/liblmdb/
      if: matrix.os == 'ubuntu-latest'
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      working-directory: ${{ runner.workspace }}/LightningQueues
      run: |
        cd ${{ runner.workspace }}/LightningQueues
        dotnet restore
    - name: Build
      run: |
        cd ${{ runner.workspace }}/LightningQueues
        dotnet build --configuration Release --no-restore
    - name: Test
      run: |
        cd ${{ runner.workspace }}/LightningQueues
        dotnet test -f netcoreapp3.1 --no-restore --verbosity normal
